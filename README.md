# CookBookManagerMod6Project
Cook Book Manager App - ITEC 2905 Module 6 Project

Added these two functions...
def add_recipe_tags(conn, cookbook_id, tags):
def track_borrowed_cookbook(conn, cookbook_id, friend_name, date_borrowed):

To add recipes run the program and wait for the program to ask you to enter the tags for each cookbook id.
Limited to change all books in order of book id. Can not select which book ids to update.

Records have been programmed to add each time the program is ran. Manually entered in records for borrowed history.
There is no functionality for user to add records. Table exists on its own and is not joined to any other table in the database.
